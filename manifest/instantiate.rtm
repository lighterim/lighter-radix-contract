CALL_METHOD
    Address("${admin_account}")
    "lock_fee"
    Decimal("10")
;
CALL_FUNCTION
    Address("${pkg}")
    "Lighter"
    "instantiate"
    Decimal("${ticket_price}")
    ${payment_window_epochs}u16
    "${relay_pub_key}"
    "${domain_name}"
;
CALL_METHOD
    Address("${admin_account}")
    "deposit_batch"
    Expression("ENTIRE_WORKTOP")
;
